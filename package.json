{
  "name": "ml-pkgs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm recursive run build",
    "generate": "npm recursive run generate",
    "publish": "pnpm recursive run publish"
  },
  "keywords": ["ml-pkgs", "utils"],
  "author": "RockyWu rocky_work@163.com",
  "license": "ISC",
  "dependencies": {
  },
  "private": "true",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "config": {
    "registry": "https://registry.npmjs.org/"
  }
}
